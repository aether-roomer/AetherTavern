"""Text-to-speech subpackage.

Holds the lookup tables for NovelAI voice presets (V1 numeric ids and V2
seed strings), the key/base-URL resolver, and helpers used by the TTS
proxy router. The router itself lives in ``server.routers.tts``.
"""
